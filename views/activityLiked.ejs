<%- include('./partials/nav') %>
<div class="container py-5">
  <h3 class="mb-4"><i class="bi bi-heart-fill text-danger me-2"></i>Liked Blogs</h3>
  <% if (!blogs || blogs.length === 0) { %>
    <div class="text-center py-5 text-muted">
      <i class="bi bi-heart display-4"></i>
      <p class="mt-3">You haven't liked any blogs yet.</p>
      <a href="/" class="btn btn-primary">Explore Blogs</a>
    </div>
  <% } else { %>
    <div class="row g-4">
      <% blogs.forEach(blog => { %>
        <div class="col-md-4">
          <!-- Reuse your existing blog card structure from home.ejs -->
          <div class="card h-100 shadow-sm">
            <% if (blog.coverImageURL) { %>
              <img src="<%= blog.coverImageURL %>" class="card-img-top" style="height:180px;object-fit:cover;">
            <% } %>
            <div class="card-body">
              <h5 class="card-title"><%= truncate(blog.title, 50) %></h5>
              <p class="card-text text-muted"><%= truncate(blog.excerpt || blog.body, 80) %></p>
              <div class="d-flex align-items-center gap-2">
                <img src="<%= blog.createdBy?.profileImageURL || '/imgs/default.png' %>" width="24" height="24" class="rounded-circle">
                <small class="text-muted"><%= blog.createdBy?.fullName %></small>
              </div>
            </div>
            <div class="card-footer bg-transparent">
              <a href="/blogs/<%= blog._id %>" class="btn btn-sm btn-outline-primary">Read</a>
              <span class="float-end text-muted"><i class="bi bi-heart-fill text-danger"></i> <%= blog.likes.length %></span>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  <% } %>
</div>
